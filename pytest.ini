[pytest]
minversion = 7.0
testpaths = tests
markers =
    edge_case: Extreme physical boundary conditions
    memory: VRAM budget and memory leak tests
    integration: End-to-end pipeline lifecycle
    cuda: Tests requiring GPU tensor math